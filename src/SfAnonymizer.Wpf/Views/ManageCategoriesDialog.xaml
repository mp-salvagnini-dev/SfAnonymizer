<Window x:Class="SfAnonymizer.Wpf.Views.ManageCategoriesDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Custom Categories"
        Width="420" Height="440"
        MinWidth="360" MinHeight="360"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize"
        Background="{StaticResource BackgroundBrush}">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Custom Categories"
                   FontSize="15" FontWeight="SemiBold"
                   Foreground="{StaticResource PrimaryBrush}"
                   Margin="0,0,0,10" />

        <!-- List of existing custom categories -->
        <Border Grid.Row="1"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="4"
                Margin="0,0,0,12">
            <ListBox x:Name="CategoriesList"
                     Background="Transparent"
                     BorderThickness="0"
                     ItemsSource="{Binding CustomCategories}"
                     SelectedItem="{Binding SelectedCategory}"
                     ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="4,3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0"
                                       Text="{Binding Name}"
                                       FontWeight="SemiBold"
                                       VerticalAlignment="Center" />
                            <TextBlock Grid.Column="1"
                                       VerticalAlignment="Center"
                                       Margin="8,0"
                                       FontSize="11"
                                       Foreground="{StaticResource SubtleTextBrush}">
                                <TextBlock.Style>
                                    <Style TargetType="TextBlock">
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding UsePrefix}" Value="True">
                                                <Setter Property="Text" Value="{Binding Prefix, StringFormat='prefix: {0}'}" />
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding UsePrefix}" Value="False">
                                                <Setter Property="Text" Value="no prefix" />
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </TextBlock.Style>
                            </TextBlock>
                            <Button Grid.Column="2"
                                    Content="✕"
                                    FontSize="11"
                                    Width="22" Height="22"
                                    Padding="0"
                                    Background="Transparent"
                                    BorderThickness="0"
                                    Foreground="{StaticResource DangerBrush}"
                                    Cursor="Hand"
                                    Tag="{Binding}"
                                    Click="RemoveCategory_Click" />
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Add new category form -->
        <Border Grid.Row="2"
                BorderBrush="{StaticResource BorderBrush}"
                BorderThickness="1" CornerRadius="4"
                Padding="12,10" Margin="0,0,0,12">
            <StackPanel>
                <TextBlock Text="Add new category"
                           FontSize="12" FontWeight="SemiBold"
                           Foreground="{StaticResource SubtleTextBrush}"
                           Margin="0,0,0,8" />

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Name"
                               VerticalAlignment="Center" FontSize="12" />
                    <TextBox Grid.Column="1" x:Name="NameBox"
                             Padding="6,4" FontSize="12"
                             BorderBrush="{StaticResource BorderBrush}" />
                </Grid>

                <Grid Margin="0,0,0,8">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="60" />
                        <ColumnDefinition Width="Auto" />
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <TextBlock Grid.Column="0" Text="Prefix"
                               VerticalAlignment="Center" FontSize="12" />
                    <CheckBox Grid.Column="1" x:Name="UsePrefixBox"
                              VerticalAlignment="Center"
                              Margin="0,0,8,0" />
                    <TextBox Grid.Column="2" x:Name="PrefixBox"
                             Padding="6,4" FontSize="12"
                             BorderBrush="{StaticResource BorderBrush}"
                             IsEnabled="{Binding IsChecked, ElementName=UsePrefixBox}" />
                </Grid>

                <Button Content="+ Add"
                        HorizontalAlignment="Left"
                        Padding="16,6"
                        Style="{StaticResource PrimaryButton}"
                        Click="AddCategory_Click" />
            </StackPanel>
        </Border>

        <!-- Footer -->
        <Button Grid.Row="3"
                Content="Close"
                HorizontalAlignment="Right"
                Padding="20,7"
                Style="{StaticResource AccentButton}"
                Click="Close_Click" />
    </Grid>
</Window>
